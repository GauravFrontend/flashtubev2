/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "res.cloudinary.com"], // Add the external domain here
  },
};

export default nextConfig;
